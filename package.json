{
  "name": "surya-mothukuri-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal portfolio. Static site generated from content/ and src/ by build.js. No framework, no dependencies.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "python3 -m http.server 8000",
    "start": "node build.js --watch & python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
